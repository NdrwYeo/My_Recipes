<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myrecipes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpdateStepActivity"></activity>
        <activity android:name=".UpdateIngredientActivity" />
        <activity android:name=".UpdateRecipeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AddIngredientActivity" />
        <activity android:name=".AddNewRecipeActivity" />
        <activity android:name=".AddStepActivity" />
        <activity android:name=".RecipeDetailActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoadingScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".DashboardActivity">

        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>